帮我更新当前项目下的 ./CLAUDE.md 和 ./README.md 文件，确保它们与最新的项目结构、构建和运行方式保持一致。

## 要求

- **CLAUDE.md**：只记录 AI 辅助开发所需的核心/重要信息（构建命令、测试、代码规范、架构、目录结构概要等）。非核心细节（如详细的 CLI 使用示例、Docker 构建参数说明、端到端测试脚本细节等）不写入 CLAUDE.md。
- **README.md**：可包含完整的构建运行说明、CLI 使用示例、Docker 指南等面向人类开发者的内容。
- 所有信息以项目实际源码为准，先探索再更新。
